{
  "name": "ats-resume-checker",
  "version": "1.0.0",
  "description": "ATS Resume Checker - Full Stack",
  "scripts": {
    "render-build": "cd frontend && rm -rf node_modules package-lock.json && npm install && cd ../backend && rm -rf node_modules package-lock.json && npm install",
    "start": "cd backend && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}